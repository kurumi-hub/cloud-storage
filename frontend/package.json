{
    "name": "frontend",
    "version": "1.0.0",
    "description": "A simple cloud storage file manager frontend built with React.",
    "main": "src/index.js",
    "scripts": {
        "start": "concurrently \"npm run server\" \"npm run client\"",
        "client": "react-scripts start",
        "build": "react-scripts build",
        "server": "node ../backend/server.js"
    },
    "dependencies": {
        "axios": "^1.8.1",
        "react": "^18.2.0",
        "react-dom": "^18.2.0",
        "react-scripts": "^5.0.1"
    },
    "devDependencies": {
        "concurrently": "^7.0.0"
    },
    "browserslist": {
        "production": [
            ">0.2%",
            "not dead",
            "not op_mini all"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version"
        ]
    }
}
